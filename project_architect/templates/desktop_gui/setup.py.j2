#!/usr/bin/env python3
"""
Setup script for {{ project_name }}

Author: {{ author_name }}{{ " <" + author_email + ">" if author_email else "" }}
{{ "Organization: " + organization if organization else "" }}
"""

from setuptools import setup, find_packages
from pathlib import Path

# Read README for long description
readme_file = Path(__file__).parent / "README.md"
long_description = readme_file.read_text(encoding="utf-8") if readme_file.exists() else ""

setup(
    name="{{ project_name | snake_case }}",
    version="{{ project_version }}",
    author="{{ author_name }}",
    {% if author_email %}author_email="{{ author_email }}",{% endif +%}
    description="{{ project_description }}",
    long_description=long_description,
    long_description_content_type="text/markdown",
    {% if organization %}url="https://github.com/{{ organization | snake_case }}/{{ project_name | snake_case }}",{% endif +%}
    packages=find_packages(),
    classifiers=[
        "Development Status :: 3 - Alpha",
        "Intended Audience :: Developers",
        "Programming Language :: Python :: 3",
        "Programming Language :: Python :: 3.12",
        "License :: OSI Approved :: MIT License",
        "Operating System :: OS Independent",
    ],
    python_requires=">=3.12",
    install_requires=[
        {% if gui_framework == 'customtkinter' %}"customtkinter>=5.2.2",
        "pillow>=10.0.0",
        {% elif gui_framework == 'pyqt6' %}"PyQt6>=6.5.0",
        {% elif gui_framework == 'pyside6' %}"PySide6>=6.5.0",
        {% elif gui_framework == 'tkinter' %}"pillow>=10.0.0",
        {% elif gui_framework == 'kivy' %}"kivy>=2.2.0",
        "kivymd>=1.1.0",
        {% endif %}
        {% if has_database %}
        {% if database_type == 'sqlite' %}"sqlalchemy>=2.0.0",
        {% elif database_type == 'postgresql' %}"psycopg2-binary>=2.9.0",
        "sqlalchemy>=2.0.0",
        {% elif database_type == 'mysql' %}"mysql-connector-python>=8.0.0",
        "sqlalchemy>=2.0.0",
        {% elif database_type == 'mongodb' %}"pymongo>=4.5.0",
        {% endif %}
        {% endif %}
        {% if has_config_file and config_format == 'yaml' %}"pyyaml>=6.0.0",{% endif +%}
        "python-dateutil>=2.8.0",
    ],
    extras_require={
        "dev": [
            {% if include_tests %}"pytest>=7.4.0",
            "pytest-cov>=4.1.0",
            "pytest-mock>=3.11.0",
            {% endif %}
            {% if include_linting %}"flake8>=6.0.0",
            "pylint>=2.17.0",
            {% endif %}
            {% if include_formatting %}"black>=23.0.0",{% endif +%}
            {% if include_type_checking %}"mypy>=1.5.0",{% endif +%}
        ],
    },
    entry_points={
        "console_scripts": [
            "{{ project_name | snake_case }}=main:main",
        ],
    },
    include_package_data=True,
    zip_safe=False,
)
