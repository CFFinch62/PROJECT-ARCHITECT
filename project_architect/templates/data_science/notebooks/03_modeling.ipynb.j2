{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# {{ project_name }} - Modeling\n",
    "\n",
    "**Author:** {{ author_name }}  \n",
    "**Date:** {{ current_date }}\n",
    "\n",
    "## Objectives\n",
    "- Train and evaluate models\n",
    "- Hyperparameter tuning\n",
    "- Model selection"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Imports\n",
    "import pandas as pd\n",
    "import numpy as np\n",
    "import matplotlib.pyplot as plt\n",
    "\n",
    "# Project imports\n",
    "import sys\n",
    "sys.path.append('..')\n",
    "from src.data_loader import load_csv, train_test_split_data\n",
    "from src.model import (\n",
    "    get_model, train_model, cross_validate,\n",
    "    tune_hyperparameters, save_model\n",
    ")\n",
{% if problem_type == 'classification' %}
    "from src.evaluate import evaluate_classification, plot_confusion_matrix, print_evaluation_report"
{% elif problem_type == 'regression' %}
    "from src.evaluate import evaluate_regression, plot_predictions, print_evaluation_report"
{% elif problem_type == 'clustering' %}
    "from src.evaluate import evaluate_clustering, print_evaluation_report"
{% else %}
    "from src.evaluate import print_evaluation_report"
{% endif %}
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 1. Load Processed Data"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Load data\n",
    "# df = load_csv('processed_data.csv', data_dir='processed')\n",
    "# X_train, X_test, y_train, y_test = train_test_split_data(df, 'target')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 2. Baseline Model"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Train baseline\n",
    "# model = get_model('default')\n",
    "# model = train_model(model, X_train, y_train)\n",
    "# y_pred = model.predict(X_test)"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Evaluate baseline\n",
{% if problem_type == 'classification' %}
    "# metrics = evaluate_classification(y_test, y_pred)\n",
    "# print_evaluation_report(metrics)\n",
    "# plot_confusion_matrix(y_test, y_pred)"
{% elif problem_type == 'regression' %}
    "# metrics = evaluate_regression(y_test, y_pred)\n",
    "# print_evaluation_report(metrics)\n",
    "# plot_predictions(y_test, y_pred)"
{% else %}
    "# print('Evaluate your model here')"
{% endif %}
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 3. Cross-Validation"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Cross-validation\n",
    "# cv_results = cross_validate(model, X_train, y_train, cv=5)\n",
    "# print(f'CV Score: {cv_results[\"mean_score\"]:.4f} (+/- {cv_results[\"std_score\"]:.4f})')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 4. Hyperparameter Tuning"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Hyperparameter tuning\n",
    "# param_grid = {\n",
    "#     'n_estimators': [50, 100, 200],\n",
    "#     'max_depth': [5, 10, None]\n",
    "# }\n",
    "# best_model, best_params = tune_hyperparameters(\n",
    "#     get_model(), X_train, y_train, param_grid\n",
    "# )\n",
    "# print(f'Best params: {best_params}')"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 5. Final Evaluation"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Final evaluation\n",
    "# y_pred_final = best_model.predict(X_test)\n",
{% if problem_type == 'classification' %}
    "# final_metrics = evaluate_classification(y_test, y_pred_final)\n",
{% elif problem_type == 'regression' %}
    "# final_metrics = evaluate_regression(y_test, y_pred_final)\n",
{% else %}
    "# final_metrics = {}\n",
{% endif %}
    "# print_evaluation_report(final_metrics)"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 6. Save Model"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Save model\n",
    "# save_model(best_model, 'final_model.pkl')"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language": "python",
   "name": "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.12.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor": 4
}

